{% extends 'layout.html.twig' %}

{% block content %}

	<h2>Contactez votre groupe !</h2>

	<section class="container align-content-center">

		<h4>Pour contacter ce groupe, merci de remplir le formulaire :</h4>

		<p>Pour toute autre demande, n'hésitez pas à nous
			<a href="/contact">
				envoyer un message par ici</a>
			! ;)</p>


		<img src="./assets/images/silhouettes.png" class="d-block mx-auto mb-2 w-25 " alt="silouhettes">

		<form class="container text-center shadow p-3 mb-5 col-lg-6 bg-body rounded" action="/validationband" method="POST">
			<div class="row g-3">
				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6 ">
					<label for="lastname" class="form-label">Nom</label>
					<input type="text" class="form-control shadow-sm p-3 mb-2 bg-body rounded" id="lastname" placeholder="Cobain" arial-label="nom" required>
					<div class="invalid-feedback">Valeur incorrecte</div>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6 ">
					<label for="firstname" class="form-label">Prénom</label>
					<input type="text" class="form-control shadow-sm p-3 mb-2 bg-body rounded" id="firstname" name="firstname" placeholder="Kurt" arial-label="prenom" required>
					<div class="invalid-feedback">Valeur incorrecte</div>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label class="form-label" for="instrument">Choisissez votre instrument</label>
					<select class="selectSearch form-select p-3 mb-3" name="instrument" id="instrument">
						<option class="optionSearch" value="" selected>-</option>
						{% for instrument in instruments %}
							<option value="{{ instrument.id }}">{{instrument.name}}</option>
						{% endfor %}
					</select>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label class="form-label" for="level">Choisissez votre niveau</label>
					{% set levels = [
					'initié',
					'débutant',
					'semi-professionnel',
        			'professionnel',
					] %}
					<select class="selectSearch form-select p-3 mb-3" id="level" name="level">
						<option class="optionSearch" selected>-</option>
						{% for level in levels %}
							<option value="{{ level }}">{{level}}</option>
						{% endfor %}
					</select>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label class="form-label" for="style">Choisissez votre style</label>
					{% set styles = [
					'Classique',
					'Jazz',
					'Variété française',
        			'Variété internationale',
					'Musiques du monde ',
					'Rap',
					'Musique électronique',
					'Rock',
					'Reggae',
					'Hard Rock',
					'Heavy Metal',
       			 	'Soul',
        			'Blues',
        			'Country',
        			'Pop',
					'R\'n\'B',
        			'Folk',
					'Alternatif',
					'Gospel'
					] %}
					<select class="selectSearch form-select p-3 mb-3" id="style" name="style">
						<option class="optionSearch" selected>-</option>
						{% for style in styles %}
							<option value="{{ style }}">{{style}}</option>
						{% endfor %}
					</select>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label class="form-label" for="localisation">Choisissez votre région</label>
					<select class="selectSearch form-select p-3 mb-3" name="localisation" id="localisation">
						<option class="optionSearch" value="" selected>-</option>
						{% for localisation in localisations %}
							<option value="{{ localisation.id }}">{{localisation.region}}</option>
						{% endfor %}
					</select>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label for="email" class="form-label">E-mail</label>
					<input type="email" class="form-control shadow-sm p-3 mb-2 bg-body rounded" id="email" placeholder="nirvana@grunge.com" arial-label="email" required>
					<div class="invalid-feedback">Valeur incorrecte</div>
				</div>

				<div class="mb-3 col-12 col-xs-12 col-md-12 col-lg-6">
					<label for="phone" class="form-label">Téléphone</label>
					<input type="text" class="form-control shadow-sm p-3 mb-2 bg-body rounded" id="phone" placeholder="06 66 09 28 10" arial-label="email" required>
					<div class="invalid-feedback">Valeur incorrecte</div>
				</div>

				<div class="mb-3 col-lg-12">
					<label for="message" class="form-label">Votre message</label>
					<textarea class="form-control shadow-sm p-3 mb-5 bg-body rounded" id="message" rows="3" placeholder="I'm so happy 'cause today I've found my friends... They're in my head. ♫" arial-label="message" required></textarea>
					<div class="invalid-feedback">Valeur incorrecte</div>
				</div>

				<div>
					<input type="submit" class="btn btn-danger col-12" id="submit"></input>
			</div>

		</div>
	</form>
</section>{% endblock %}
